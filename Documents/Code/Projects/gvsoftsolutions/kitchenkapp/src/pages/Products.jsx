import React from "react";
import InventoryAdmin from "../components/InventoryAdmin.jsx";

export default function Products(props) {
  // pass navigation or other props through if needed
  return <InventoryAdmin {...props} />;
}
